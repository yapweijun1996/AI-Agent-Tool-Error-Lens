# Changelog

All notable changes to Agent Error Lens are recorded here. The package has not been published to npm.

## [Unreleased]

- froze the V0.1 request, result, diagnostic, evidence, producer, confidence, redaction, path, budget, and serialization contracts;
- implemented the bounded TypeScript, Vitest, ESLint, generic structured, and generic text parsing paths;
- added canonical library and CLI interfaces with deterministic output, evidence mapping, redaction, deduplication, and explicit parser/producer status separation;
- added package boundary checks, packed consumer/CLI smoke tests, agent-facing evidence handoff tests, and the approved Node/OS verification matrix;
- selected and implemented the MIT license;
- prepared npm metadata, release documentation, and a manual tag-bound release workflow.

## Release policy

An entry moves from `Unreleased` to a versioned section only after the exact release tag, packed artifact, npm registry metadata, integrity, and `gitHead` have been read back and recorded in the Core SSOT documents.
